# MCAT Speedrun — AI answer-grader evaluation harness.
# Intentionally empty: modules inside this package are imported by bare name
# (flat, same-dir) after the package dir is placed on sys.path by run_eval.py
# and the pytest entrypoint. See README.md for the contract.
